# Optional: enables LLM-powered bot messages (GPT-4o-mini) — toggle on via /admin panel
OPENAI_API_KEY=

# Password for the admin panel at /admin
ADMIN_PASSWORD=

# CORS origin for production (default: https://clawsoc.io)
# Not needed for local development
CORS_ORIGIN=
